# Enter the Cradle

Enter the Cradle is a personalized immersive web experience where users upload a selfie and become part of a cinematic Niiro ride.

## Local media

Source footage belongs in `public/source-footage/`, and generated videos belong in `output/`. Both directories are intentionally excluded from version control so large media files are never committed.
